/**
 * Este arquivo é um placeholder para os tipos que serão gerados automaticamente
 * pelo CLI do TinaCMS. Em produção, esses tipos serão gerados com base nas
 * definições das suas coleções no config.ts.
 */

export const queries = {
    // Funções de consulta básicas que serão substituídas pelo código gerado
    template1Connection: async (variables) => {
      console.warn("Usando cliente TinaCMS não gerado - execute 'npx tinacms dev' para gerar o cliente completo");
      return {
        data: {
          template1Connection: {
            edges: []
          }
        }
      };
    },
    template2Connection: async (variables) => {
      console.warn("Usando cliente TinaCMS não gerado - execute 'npx tinacms dev' para gerar o cliente completo");
      return {
        data: {
          template2Connection: {
            edges: []
          }
        }
      };
    },
    // Funções de criação para o gerador de páginas
    createTemplate1: async (variables) => {
      console.warn("Usando cliente TinaCMS não gerado - execute 'npx tinacms dev' para gerar o cliente completo");
      return null;
    },
    createTemplate2: async (variables) => {
      console.warn("Usando cliente TinaCMS não gerado - execute 'npx tinacms dev' para gerar o cliente completo");
      return null;
    }
  };